[package]
name = "sl-bake"
version = { workspace = true }
authors = { workspace = true }
edition = { workspace = true }
rust-version = { workspace = true }
description = "Client-side avatar bake compositing (ordered wearable layers → per-region baked RGBA) for Second Life / OpenSim clients, the legacy/OpenSim path"
readme = { workspace = true }
license = { workspace = true }

repository = { workspace = true }
keywords = ["avatar", "bake", "compositing", "opensim", "secondlife"]
categories = ["game-development", "rendering", "rendering::data-formats"]
publish = { workspace = true }

exclude = { workspace = true }

[dependencies]
bytes = { workspace = true }
sl-proto = { workspace = true }
sl-texture = { path = "../sl-texture", version = "0.1.0", default-features = false }

[dev-dependencies]
pretty_assertions = { workspace = true }

[lints]
workspace = true
