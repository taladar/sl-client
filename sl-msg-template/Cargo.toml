[package]
name = "sl-msg-template"
version = { workspace = true }
authors = { workspace = true }
edition = { workspace = true }
rust-version = { workspace = true }
description = "Parser for the Second Life / OpenSim LLUDP message_template.msg into a typed AST"
readme = { workspace = true }
license = { workspace = true }

repository = { workspace = true }
keywords = ["lludp", "opensim", "parser", "protocol", "secondlife"]
categories = ["encoding", "parser-implementations"]
publish = { workspace = true }

exclude = { workspace = true }

[dependencies]
thiserror = { workspace = true }

[dev-dependencies]
# The integration test reads the vendored template from `sl-wire` at run time;
# `clippy.toml` disallows `std::fs` in favour of `fs_err`.
fs-err = { workspace = true }
pretty_assertions = { workspace = true }

[lints]
workspace = true
