---
id: test-texture-fetch-http
title: HTTP CAPS texture fetch + decode (extends asset-decode)
topic: test
status: done
origin: TEST_ROADMAP.md — Phase 13 — Asset & texture pipeline `[both]`
---

Context: [context/test.md](../context/test.md).

`texture-fetch-http` — HTTP CAPS texture fetch + decode (extends
`asset-decode`). `1av`.
